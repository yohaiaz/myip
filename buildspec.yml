version: 0.2

phases:
  install:
    commands:
      - echo install phase,  on `date`
  pre_build:
    commands:
      - echo pre_build phase,  on `date`
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - myip